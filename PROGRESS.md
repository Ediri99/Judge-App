# Progress

## Phase 0
- Created a Vite + React + TypeScript project scaffold.
- Added the shared design-token layer and prototype-inspired UI components.
- Implemented a judge shell with the Sign In / List / Score routes and an admin shell with empty panels.
- Added PWA configuration, an env example, and README run steps.

## Decisions
- Kept the initial implementation as a visual scaffold without real data, auth, or scoring logic.
- Used the prototype palette and spacing as the baseline for the design-system shell.

## Follow-ups
- Add real Firebase wiring and data/model work in Phase 1.
- Replace shell placeholders with real screens once Phase 1 and later phases begin.
- Resolve the npm registry install issue in this environment before the build can be fully verified.
